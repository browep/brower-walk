Dependencies.  You need the Android SDK installed and licenses accepted.  You will also need Java 1.8 installed.

From the repo route

```
cd android
./gradlew clean build
```

Installable APKs will be in `./app/build/outputs/apk`

